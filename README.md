# 🌱 AgroBot — Telegram бот для планирования посева

## Структура проекта
```
agro_bot/
├── bot.py          # точка входа
├── config.py       # конфиг (читает .env)
├── states.py       # FSM состояния
├── handlers.py     # вся логика бота
├── keyboards.py    # кнопки и клавиатуры
├── texts.py        # тексты RU/EN
├── agro.py         # анализ поля + Groq LLM
├── requirements.txt
├── .env.example
└── README.md
```

## Установка и запуск

### 1. Получи токен бота
- Открой [@BotFather](https://t.me/BotFather) в Telegram
- Напиши `/newbot` и следуй инструкциям
- Скопируй токен вида `123456789:AAF...`

### 2. Получи Groq API ключ
- Зарегистрируйся на [console.groq.com](https://console.groq.com)
- Создай API Key

### 3. Настрой переменные окружения
```bash
cp .env.example .env
# Открой .env и вставь свои ключи
```

### 4. Установи зависимости
```bash
pip install -r requirements.txt
```

### 5. Запусти бота
```bash
python bot.py
```

## Команды бота
- `/start` — начать, выбрать язык
- `/newfield` — добавить новое поле
- `/help` — справка

## Что делает бот
1. Спрашивает название поля
2. Просит геолокацию или координаты
3. Спрашивает площадь (ha, m2, ac)
4. Тип почвы (inline кнопки)
5. pH, органику, глину, песок (можно пропустить)
6. Заметки (можно пропустить)
7. Запрашивает погоду (Open-Meteo) и климат (NASA POWER)
8. Отправляет всё в Llama 3.3 через Groq
9. Возвращает красиво отформатированный план посева
